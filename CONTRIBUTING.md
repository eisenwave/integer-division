# Contributing guidelines

Please make sure that your code is correctly formatted using ClangFormat 19,
and that it matches the surrounding style.

Also run the tests in `test.cpp` by building the `test` executable with CMake.
